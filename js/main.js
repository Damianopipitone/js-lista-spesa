const list = ['Pane', 'Formaggio', 'Yogurt', 'Pomodori','Nutella'];

/*for (let i = 0; i < list.length; i++) {
    console.log(list[i]);
}; */

let i = 0;

while (list[i]) {
    
    console.log(i+1 + '.' + list[i]);
    i++;
};